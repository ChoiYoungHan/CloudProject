version: 0.2

env:
  variables:
    IMAGE_NAME: main-portal
    REGION: us-west-2
    ECR_REPO: 207567776727.dkr.ecr.us-west-2.amazonaws.com

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region $REGION | docker login --username AWS --password-stdin $ECR_REPO
  build:
    commands:
      - echo Building the Docker image...
      - echo Current directory:
      - pwd
      - echo Directory contents:
      - ls -la
      - echo Moving to main_portal directory...
      - cd main_portal
      - echo Current directory after cd:
      - pwd
      - echo Directory contents after cd:
      - ls -la
      - echo IMAGE_NAME is $IMAGE_NAME
      - docker build -t $IMAGE_NAME -f Dockerfile .
  post_build:
    commands:
      - echo Tagging and pushing the Docker image...
      - docker tag $IMAGE_NAME:latest $ECR_REPO/$IMAGE_NAME:latest
      - docker push $ECR_REPO/$IMAGE_NAME:latest